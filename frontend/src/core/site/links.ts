/**
 * Canonical outbound links used across the marketing surfaces (header, footer,
 * pricing page). Keep them here so a repo move only needs a single edit.
 */
export const GITHUB_REPO_URL = "https://github.com/Muhabuki003/bookistudios-ai";

export const GITHUB_ISSUES_URL = `${GITHUB_REPO_URL}/issues`;
